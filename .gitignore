# WebDriverAgent is cloned by setup-wda.sh, never committed
wda/
.wda-derived/

# State files generated at runtime
*.log
*.pid
.installed

# User snapshots (per-app, never commit)
snapshots/

# Explore-zone outputs (per-run, never commit)
explore/

# macOS
.DS_Store

# IntelliJ
.idea/
